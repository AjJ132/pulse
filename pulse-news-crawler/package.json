{
  "name": "pulse-news-crawler",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "clean-build-dir": "rm -rf resources/terraform/build ",
    "build": "npm run clean-build-dir && tsc --project tsconfig.json",
    "apply": "npm run build && cd resources/terraform && terraform apply --auto-approve",
    "dev": "AWS_REGION=us-east-1 ts-node local-wrapper/server.ts",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.25.2",
    "@aws-lambda-powertools/metrics": "^2.25.2",
    "@aws-lambda-powertools/tracer": "^2.25.2",
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/client-sns": "^3.470.0",
    "@aws-sdk/util-dynamodb": "^3.470.0",
    "dotenv": "^17.2.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
